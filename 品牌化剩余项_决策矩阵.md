# 品牌化剩余项决策矩阵

> 基线：`K` 分支，`HEAD = 50bbdd433`（`Centralize Phase 4 compatibility branding`）
>
> 说明：本文基于**当前工作树真实状态**整理，判断时包含本地未提交改动的现状；目标是给“最终品牌化”提供一份可执行的决策矩阵，而不是重复旧计划文档。

## 1. 决策标签说明

### 1.1 当前状态

- `已抽象但未切值`：已有中心入口或模板，但默认值仍保留旧 RenderDoc 身份。
- `已抽象且已切值，但未覆盖验证`：代码入口已切到当前品牌，但仍缺少该方向的专门回归。
- `尚未抽象`：仓库里仍有分散硬编码，未来若要深度品牌化，需要先抽象再改值。
- `混合`：同一类问题里，一部分已经抽象/切值，另一部分仍保留旧名或旧协议字面量。

### 1.2 建议决策

- `保留旧值`：把它当成协议/格式/生态兼容层的一部分，最终品牌化时也不强行改。
- `双轨兼容`：新增新品牌值，但保留旧入口或旧识别一段时间，建议“旧读 + 新写”或“新旧都认”。
- `切新值`：作为最终品牌的一部分直接切成新品牌，不再保留旧 RenderDoc 身份。

## 2. 总体建议矩阵

| 大类 | 默认建议 | 原因 |
| --- | --- | --- |
| 文件格式与 capture 协议 | `保留旧值` 或 `双轨兼容` | 这类改动会直接影响 `.rdc` 生态、外部工具、旧版可读性 |
| 原生 ABI / 导出符号 / replay marker | `双轨兼容` | 外部程序、示例、插件、脚本可能直接依赖 `RENDERDOC_*` |
| Vulkan / Android 外部识别 | `切新值`，但保留必要兼容入口 | 这是最终品牌最显眼的系统身份，但 loader / APK / 注册需要回归 |
| 系统集成（安装器 / 关联 / shell / desktop） | `切新值` | 这是用户视角下最应该完成的品牌化收尾 |
| Python / docs / tests 生态 | `双轨兼容` | 运行时和脚本层最适合先兼容后收紧 |
| URL / 邮件 / issue / analytics / bugreport | `切新值` | 低风险，且决定对外身份归属 |

## 3. 逐项决策矩阵

## 3.1 高风险（兼容性敏感）

| 项目 | 逻辑含义 | 当前状态 | 推荐决策 | 关键文件 | 最小必要验证 |
| --- | --- | --- | --- | --- | --- |
| `.rdc` / `.cap` 扩展名与 file type | 决定 capture / settings 文件如何命名、筛选、打开、保存 | `已抽象但未切值` | 默认 `保留旧值`；若要深度品牌化，采用 `双轨兼容` | `renderdoc/common/brand_config.h:61` `renderdoc/replay/capture_file.cpp:226` `qrenderdoc/Windows/MainWindow.cpp:603` `qrenderdoc/Widgets/ReplayOptionsSelector.cpp:205` | 新旧 capture 双开；保存/另存/重写；CLI `thumb/replay`；拖拽打开 |
| `RDOC` 文件头 magic | 决定 `.rdc` 真正的格式签名，不只是扩展名 | `已抽象但未切值` | 默认 `保留旧值`；若确实要换，必须 `双轨兼容`（旧读 + 新写） | `renderdoc/common/brand_config.h:66` `renderdoc/serialise/rdcfile.cpp:143` `renderdoc/os/win32/win32_shellext.cpp:186` | 旧文件可读；新文件可写；缩略图可读；旧工具/旧版兼容边界明确 |
| Capture section 名（`renderdoc/internal/*`、`renderdoc/ui/*`） | `.rdc` 内部 section 类型名，属于容器协议语义 | `尚未抽象` | 默认 `保留旧值`；除非准备维护分叉格式，否则不建议切新 | `renderdoc/api/replay/renderdoc_tostr.inl:1120` | 带 notes / bookmarks / shader edits 的 capture 写入再打开；结构化 section 检查 |
| 原生 API 头名、`RENDERDOC_*` / `pRENDERDOC_*` 族 | 对外 C API / Replay API 的主 ABI 名称 | `已抽象，但旧 ABI 仍是主入口` | `双轨兼容`；继续保留原生 `RENDERDOC_*`，新品牌作为 compat alias | `renderdoc/replay/app_api.cpp:457` `renderdoc/api/replay/renderdoc_replay.h:52` `renderdoc/api/replay/compat_config.h:6` | 旧 header / 新 compat header 都可编译；旧 `GetProcAddress` / 新 alias 都成功 |
| `RENDERDOC_GetAPI` / compat `GetAPI` 导出 | 外部程序动态获取 API 的唯一核心入口之一 | `已抽象，但生成链仍手工` | `双轨兼容` | `renderdoc/replay/app_api.cpp:463` `renderdoc/replay/entry_points.cpp:547` `util/generate_brand_compat.py:24` | 动态加载旧名与新名都能成功；self-host capture 正常 |
| replay marker（`renderdoc__replay__marker`） | 防止 replay 程序被误抓帧的 ABI 识别点 | `已抽象，但旧 marker 仍保留` | `双轨兼容` | `renderdoc/api/replay/renderdoc_replay.h:52` `renderdoc/api/replay/compat_config.h:8` | 用旧 marker / 新 marker 标记的程序都不被 capture |
| POSIX 导出脚本中的 `RENDERDOC_*` / `VK_LAYER_RENDERDOC_*` | Linux/macOS 上真正导出的符号集合 | `尚未抽象` | `双轨兼容` | `renderdoc/renderdoc.version:20` `renderdoc/rdocself.version.in:20` `renderdoc/rdocself.version:20` | `nm -D` / `readelf -Ws` 检查导出；Vulkan layer 可被 loader 找到 |
| 注入 / hook 环境变量（`RENDERDOC_CAPFILE` 等） | POSIX 注入链用它把 capture 参数从 launcher 传给被注入进程 | `尚未抽象` | `双轨兼容`；建议未来支持“读旧 + 读新，写新” | `renderdoc/os/posix/posix_process.cpp:793` `renderdoc/os/posix/posix_libentry.cpp:58` `renderdoc/os/posix/posix_stringio.cpp:225` `renderdoc/driver/gl/egl_hooks.cpp:1030` | Linux/macOS `Launch + Inject`；child process 继承；`RENDERDOC_HOOK_EGL` 行为 |
| 日志协议字头 `RDOC` / Android logcat 解析 | UI LogView 与 Android log 解析依赖该字头识别“Core”日志 | `尚未抽象` | 默认 `保留旧值`；若要改，必须成套修改解析器与生成端 | `renderdoc/common/common.h:460` `renderdoc/android/android_utils.cpp:484` `qrenderdoc/Windows/LogView.cpp:524` | Windows/Linux/UI log；Android logcat 抓取与解析 |
| Vulkan disable env var / Android system property 残留旧名 | 已经切了主 layer 名，但仍有旧生态名字作为控制面 | `混合` | `双轨兼容` | `renderdoc/driver/vulkan/renderdoc.json:45` `renderdoc/android/android.cpp:1225` | Vulkan layer 注册、Android 权限自动授予、老脚本是否仍能工作 |

### 高风险决策建议

- 第一优先级是**明确边界**：你要的是“外显品牌化”，还是“连格式/ABI/协议都去 RenderDoc 化”。
- 如果目标是“稳定 fork + 自有品牌”，建议：
  - `.rdc` / `.cap` / `RDOC` / section 名：先不改。
  - `RENDERDOC_*`：保留原生主 ABI，新品牌继续走 compat alias。
  - 注入 env / 日志协议：先兼容旧名，再视需要引入新名。

## 3.2 中风险

| 项目 | 逻辑含义 | 当前状态 | 推荐决策 | 关键文件 | 最小必要验证 |
| --- | --- | --- | --- | --- | --- |
| Vulkan layer 名、函数前缀、JSON、Android 包名 / so 名 | Vulkan loader、Android 包管理器和外部工具看到的主品牌身份 | `混合`，主路径已抽象并切值，但仍有个别旧名残留 | `切新值`，并保留必要兼容入口 | `renderdoc/common/brand_config.h:100` `renderdoc/common/globalconfig.h:155` `renderdoc/driver/vulkan/renderdoc.json:4` `renderdoccmd/android/AndroidManifest.xml:3` | Windows/Linux layer 注册；`vkEnumerateInstanceLayerProperties`；Android APK 安装启动 |
| Windows ProgID / 文件关联 / OpenWith | 决定系统里 `.rdc` / `.cap` 由谁打开、显示什么描述 | `已抽象且已切值，但未覆盖验证` | `切新值` | `util/installer/Branding.wxi:26` `util/installer/Installer64.wxs:44` | 安装后双击打开；右键 OpenWith；升级/卸载后关联是否干净 |
| Shell extension / thumbnail handler CLSID | 决定 Explorer 缩略图处理器的 COM 身份 | `混合`，显示名已抽象，CLSID 固定 | 若需要与上游并存，`切新值`；若只替换安装，可暂时 `保留旧值` | `util/installer/Installer64.wxs:87` `renderdoc/os/win32/win32_shellext.cpp:45` | 与上游并装；Explorer 缩略图；升级覆盖路径 |
| 命名对象 / event / pipe / shared memory / crash / update 名称 | 进程间协作的命名约定，如 global hook、crash handle、breakpad pipe、update tempdir | `已抽象且已切值，但未覆盖验证` | `切新值` | `renderdoc/common/brand_config.h:37` `renderdoc/common/brand_config.h:53` `renderdoc/core/crash_handler.h:134` `renderdoccmd/renderdoccmd_win32.cpp:606` | 崩溃处理；global hook；update 替换；helper 自举 |
| POSIX 配置目录 `~/.renderdoc/` | Linux/macOS 用户配置与 remoteserver 白名单等默认落盘位置 | `尚未抽象` | `双轨兼容`；建议“先读旧目录，再写新目录” | `renderdoc/os/posix/linux/linux_stringio.cpp:610` `renderdoc/os/posix/bsd/bsd_stringio.cpp:610` `renderdoc/os/posix/apple/apple_stringio.cpp:155` `renderdoc/core/remote_server.cpp:1141` | Linux/macOS 启动、配置读写、remote server 白名单 |
| `share/renderdoc/*` 安装路径、插件路径、pylibs 路径 | 安装后 Python、SPIR-V 插件、Android plugin 包查找都依赖这些路径 | `尚未抽象` | `双轨兼容` 或 `切新值`，取决于是否允许与旧安装目录共存 | `renderdoc/core/plugins.cpp:51` `qrenderdoc/Code/pyrenderdoc/PythonContext.cpp:251` `qrenderdoc/Code/Interface/PersistantConfig.cpp:562` `renderdoc/android/android.cpp:446` | Linux/macOS 安装后 Python shell、SPIR-V 工具、Android plugin 查找 |
| Linux desktop / MIME / thumbnailer / menu | 桌面环境里最直观的品牌身份 | `尚未抽象` | 如果保留 `.rdc`，可分阶段；最终建议 `切新值` | `qrenderdoc/share/renderdoc.desktop:3` `qrenderdoc/share/renderdoc.thumbnailer:2` `qrenderdoc/share/renderdoc-capture.xml:3` `qrenderdoc/share/menu:1` | 安装后桌面启动项、缩略图、MIME 识别、菜单图标 |
| Python alias、docs 校验脚本、tests 仍兼容旧模块名 | 当前运行时和文档脚本都允许 `renderdoc` / `qrenderdoc` 继续成立 | `已抽象且已切值，但兼容层仍保留旧名` | `双轨兼容` | `qrenderdoc/Code/pyrenderdoc/PythonContext.cpp:303` `docs/verify-docstrings.py:249` `docs/verify-docstrings.py:41` | 新旧模块名 import；stub / docstring 生成；至少一条 Python 测试路径 |

### 中风险决策建议

- 这一层最适合做成“**外显身份全切新，内部保留少量兼容**”。
- 如果你计划和上游 RenderDoc 并装，优先把下面三件事明确下来：
  - Shell extension 的 `CLSID` 是否换新；
  - POSIX 配置目录是否迁移到新目录名；
  - Linux `share/renderdoc/*` 是否整体改成新目录并保留旧路径 fallback。

## 3.3 低风险

| 项目 | 逻辑含义 | 当前状态 | 推荐决策 | 关键文件 | 最小必要验证 |
| --- | --- | --- | --- | --- | --- |
| 官网 / docs / builds / source / issues / email | 对外身份、帮助入口、升级与反馈路径 | `已抽象但未切值` | `切新值` | `renderdoc/common/brand_config.h:76` `util/rdoc_brand.py:40` `qrenderdoc/Code/QRDUtils.cpp:196` | About / Help / Update / Tips / Crash 中所有链接可点通 |
| bugreport / analytics / tips / update 端点 | UI 外围服务身份与隐私说明落点 | `已抽象但未切值` | `切新值` | `renderdoc/common/brand_config.h:87` `renderdoc/common/brand_config.h:115` `util/installer/Branding.wxi:16` | 更新检查、tips、analytics、bugreport 链接是否都指向新域 |
| 静态 UI / CLI 文案残留 | 用户可见的旧品牌，但通常不影响协议和兼容 | `混合`，有些走了 UI 替换层，有些仍硬编码 | `切新值` | `renderdoccmd/renderdoccmd.cpp:1538` `renderdoccmd/renderdoccmd.rc:101` `qrenderdoc/Windows/Dialogs/SettingsDialog.ui:433` | CLI `--help`、About、Settings、Update、Analytics、Crash 文案 |
| `renderdoccmd` 窗口标题 / 资源描述 | 主要是小型前端与资源层身份 | `尚未抽象` | `切新值` | `renderdoccmd/renderdoccmd_linux.cpp:221` `renderdoccmd/renderdoccmd_apple.cpp:51` `renderdoccmd/renderdoccmd.rc:101` | Linux/Apple 预览窗口标题、Windows 文件属性 |
| 制造商 / 作者身份 | 安装器、About、analytics prompt、帮助页中“谁发布的”这层身份 | `已抽象但未切值` | 取决于品牌归属；若是独立 fork，建议 `切新值` | `renderdoc/common/brand_config.h:76` `util/installer/Branding.wxi:13` | 安装器属性、About、Analytics Prompt、Crash 联系方式 |
| 内部错误消息 / marker / 日志文案中的 `RenderDoc` | 对用户可见，但更多是文案层残留，不是核心协议 | `尚未抽象` | `切新值` 或延后 | `renderdoc/api/replay/renderdoc_tostr.inl:55` `renderdoc/driver/vulkan/vk_core.cpp:306` `renderdoc/driver/vulkan/wrappers/vk_device_funcs.cpp:669` | 错误弹窗、Vulkan 调试输出、日志页展示 |

### 低风险决策建议

- 这一层可以在不动 ABI / 格式的前提下持续收尾。
- 如果你想尽快让用户“看起来已经不是 RenderDoc”，这一组的收益最高、返工最小。

## 4. 推荐决策组合

## 4.1 方案 A：稳定品牌化（推荐）

- `保留旧值`：`.rdc`、`.cap`、`RDOC` magic、capture section 名、`RENDERDOC_*` 主 ABI、日志 `RDOC` tag。
- `双轨兼容`：compat `GetAPI`、compat replay marker、Python alias、POSIX env、POSIX 配置目录、Linux 安装/share 路径。
- `切新值`：可执行文件名、DLL / so 名、UI / cmd / shim 品牌、Vulkan layer 主名、Android 包名 / so 名、安装器身份、链接和邮件、桌面集成。

### 适用场景

- 你要的是“独立品牌的稳定 fork”；
- 仍希望最大限度兼容既有 capture、脚本和外部集成；
- 不准备维护一套完全分叉的文件格式和原生 ABI。

## 4.2 方案 B：深度去 RenderDoc 特征

- 在方案 A 基础上，继续把 `.rdc` / `.cap`、`RDOC`、section 名、POSIX env、日志 tag、MIME 名、可能的 CLSID 一并切新。
- 此方案必须额外做：
  - capture 双读单写迁移；
  - ABI 双导出过渡；
  - Linux MIME / magic / desktop / thumbnailer 全量同步；
  - Android / Vulkan 老旧控制面兼容处理；
  - 与上游 RenderDoc 并存与互操作边界说明。

### 适用场景

- 你准备长期维护自己的生态；
- 不再追求与上游的直接协议/ABI 兼容；
- 能接受一次完整的格式与生态迁移成本。

## 5. 我建议先拍板的 8 个问题

1. `.rdc` / `.cap` 是否仍保留旧扩展名？
2. `RDOC` 文件头 magic 是否保留旧值？
3. capture section 名是否继续保留 `renderdoc/internal/*`？
4. `RENDERDOC_*` 是否永远保留为主 ABI，还是仅保留过渡期？
5. POSIX 注入 env 是否引入新品牌名，并做旧名 fallback？
6. Shell extension `CLSID` 是否换新，以支持与上游并存？
7. POSIX 配置目录 / `share/renderdoc/*` 是否迁移到新路径名？
8. Python `renderdoc` / `qrenderdoc` alias 是否在最终品牌版中继续保留？
针对整理的内容，我的意见如下:1..rdc / .cap 仍保留旧扩展名. 2.RDOC 文件头 magic 不保留旧值,跟随当前设计自定义系统重命名逻辑修改为ripperk系列命名.

## 6. 最小执行顺序建议

1. 先按本文拍板高风险项边界。
2. 再完成中风险里的系统集成与 POSIX 路径策略。
3. 最后持续清理低风险文案、链接、外围身份。

如果后续要进入实现阶段，建议先从“方案 A：稳定品牌化”推进，再决定是否值得进入“方案 B：深度去 RenderDoc 特征”。
